% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_mode.R
\name{calc_mode}
\alias{calc_mode}
\title{Calculate Mode}
\usage{
calc_mode(x)
}
\arguments{
\item{x}{A numeric vector.}
}
\value{
A numeric vector of mode value(s). Returns multiple values when
there is a tie (multimodal). Returns \code{NA} with a message when
multiple distinct values all occur with equal frequency (no unique mode).
}
\description{
Returns the most frequently occurring value(s) in a numeric vector.
}
\details{
\itemize{
\item NA values are silently removed before computation.
\item If all distinct values appear with equal frequency and there is more
than one distinct value (e.g., \code{c(1, 2, 3)}), the function
returns \code{NA} and emits a message.
\item A single distinct value (e.g., \code{c(5, 5, 5)}) is the mode.
\item If multiple values share the highest frequency, all are returned
(multimodal case).
\item An error is raised if \code{x} is not numeric, is empty, or contains
only NA values.
}
}
\examples{
calc_mode(c(1, 2, 2, 3))
# [1] 2

calc_mode(c(1, 1, 2, 2, 3))
# [1] 1 2

calc_mode(c(1, 2, 3))
# [1] NA  (no unique mode)

calc_mode(c(1, 2, 2, 3, 4, 5, 5, 5, 6, 10))
# [1] 5

}
\seealso{
Other descriptive statistics: 
\code{\link{calc_iqr}()},
\code{\link{calc_mean}()},
\code{\link{calc_median}()},
\code{\link{calc_q1}()},
\code{\link{calc_q3}()}
}
\concept{descriptive statistics}
